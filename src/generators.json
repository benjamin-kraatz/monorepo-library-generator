{
  "generators": {
    "contract": {
      "factory": "./src/generators/contract/contract",
      "schema": "./src/generators/contract/schema.json",
      "description": "Generate a contract library with entities, events, ports, and errors following Effect-based architecture"
    },
    "provider": {
      "factory": "./src/generators/provider/provider",
      "schema": "./src/generators/provider/schema.json",
      "description": "Generate a provider library with Effect-based architecture for external service integration"
    },
    "data-access": {
      "factory": "./src/generators/data-access/data-access",
      "schema": "./src/generators/data-access/schema.json",
      "description": "Generate a repository-oriented data access library with Effect patterns"
    },
    "feature": {
      "factory": "./src/generators/feature/feature",
      "schema": "./src/generators/feature/schema.json",
      "description": "Generate a feature library with Effect-based architecture"
    },
    "infra": {
      "factory": "./src/generators/infra/infra",
      "schema": "./src/generators/infra/schema.json",
      "description": "Generate an infrastructure library with Effect-based architecture"
    }
  }
}
